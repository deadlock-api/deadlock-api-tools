services:
  active-matches-scraper:
    build:
      context: active-matches-scraper
    restart: always
    env_file: .env

  ingest-worker:
    build:
      context: ingest-worker
    restart: always
    env_file: .env
    stop_grace_period: 30s

  matchdata-downloader:
    build:
      context: matchdata-downloader
    restart: always
    env_file: .env
    stop_grace_period: 30s

  salt-scraper:
    build:
      context: salt-scraper
    restart: always
    environment:
      RUST_LOG: debug
    env_file: salt-scraper/.env

  mmr-calc:
    build:
      context: mmr-calc
    restart: always
    env_file: .env

  rank-fetcher:
    build:
      context: rank-fetcher
    restart: always
    env_file: .env

  history-fetcher:
    build:
      context: history-fetcher
    restart: always
    env_file: .env

  builds-fetcher:
    build:
      context: builds-fetcher
    restart: always
    env_file: .env

  hltv-spectator:
    build:
      context: hltv-scraper
    restart: always
    env_file: .env

  hltv-scraper:
    build:
      context: hltv-scraper
    restart: always
    env_file: .env
    volumes:
    - ./local-store:/matches
